server:
  port: 19094

spring:
  application:
    name: order-service

  datasource:
    username: admin
    url: jdbc:postgresql://localhost:5432/postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/